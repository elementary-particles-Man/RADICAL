# RADICAL media compatibility path

This directory is intentionally present so repository-wide release media validation can scan the historical top-level path without treating it as missing.
Active RADICAL installer media logic lives in `projects/tuff-linux-distro/build/live-build/`, `config/`, and `release/installer/`.
